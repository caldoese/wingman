specVersion: "v1beta"
serviceId: "wingman"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "wingman-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
